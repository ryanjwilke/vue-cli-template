version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:11.15.0
    steps:
      - checkout
      - run:
          name: List
          command: ls -la
      - run:
          name: Yarn Install
          command: yarn install
      - run:
          name: CI Tests
          command: yarn test
