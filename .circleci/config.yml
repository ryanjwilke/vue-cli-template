version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:11.15.0
    steps:
      - checkout
      - run:
          name: Yarn Install
          command: npm install yarn -g
      - run:
          name: List
          command: ls -la
      - run:
          name: CI Tests
          command: yarn test
