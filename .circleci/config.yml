version: 2.1
jobs:
  build:
    docker:
      - image: node:11.15.0
    steps:
      - run:
          name: Yarn Install
          command: npm install yarn -g
      - run:
          name: CI Tests
          command: yarn test
